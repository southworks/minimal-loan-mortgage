{
  "$schema": "https://schema.management.azure.com/schemas/0.1.2-preview/CreateUIDefinition.MultiVm.json#",
  "handler": "Microsoft.Azure.CreateUIDef",
  "version": "0.1.2-preview",
  "parameters": {
    "basics": [
      {
        "name": "location",
        "type": "Microsoft.Common.Location",
        "label": "Region",
        "toolTip": "Azure region for the demo environment."
      },
      {
        "name": "baseName",
        "type": "Microsoft.Common.TextBox",
        "label": "Base name",
        "defaultValue": "cohereloan",
        "toolTip": "Short prefix used for deployed resource names."
      },
      {
        "name": "nameSuffix",
        "type": "Microsoft.Common.TextBox",
        "label": "Name suffix (retry only)",
        "defaultValue": "",
        "toolTip": "Leave empty on first deploy. If redeploying after a partial failure, enter a short value such as 2 or 3 to avoid name conflicts with resources stuck in deletion."
      }
    ],
    "outputs": {
      "location": "[location()]",
      "baseName": "[basics('baseName')]",
      "nameSuffix": "[basics('nameSuffix')]"
    }
  }
}
